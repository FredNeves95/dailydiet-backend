import { describe, it, expect } from 'vitest'

describe('Meal repository tests', () => {
  it('should list all user meals after when calling findAll method', async () => {
    expect(2).toEqual(2)
  })
})
